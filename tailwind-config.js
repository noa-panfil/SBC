// tailwind-config.js
tailwind.config = {
    theme: {
        extend: {
            colors: {
                sbc: {
                    light: '#4ade80',  //Vert clair
                    DEFAULT: '#15803d', //Vert principal
                    dark: '#14532d',    //Vert foncé
                }
            },
            fontFamily: {
                sans: ['Inter', 'sans-serif'],
            }
        }
    }
}